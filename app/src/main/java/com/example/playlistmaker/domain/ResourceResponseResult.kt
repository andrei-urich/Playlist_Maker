package com.example.playlistmaker.domain

import com.example.playlistmaker.EMPTY_STRING

object ResourceResponseResult {
    const val SUCCESS = "SUCCESS"
    const val ERROR = "ERROR"
    var resourceResponseResult = EMPTY_STRING
}